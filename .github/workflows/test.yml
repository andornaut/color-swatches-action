name: Generate example swatches

on:
  push:
    branches: [main]
    paths:
      - README.md
      - color-swatches.mjs
      - action.yml

  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./
        with:
          files: 'README.md'
          out-dir: 'swatches'
