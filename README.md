# color-swatches-action

A GitHub Action and CLI tool that extracts hex color codes from files and generates color swatch images (SVG or PNG).

Supports all CSS/VS Code hex formats: `#RGB`, `#RGBA`, `#RRGGBB`, and `#RRGGBBAA`.

## Example

The swatches below are generated by the [test workflow](.github/workflows/test.yml),
which runs this action against this README itself.

| Color | Hex | Swatch |
| ----- | --- | ------ |
| Red | `#ff0000` | ![#ff0000](swatches/ff0000.svg) |
| Green | `#00ff00` | ![#00ff00](swatches/00ff00.svg) |
| Blue | `#0000ff` | ![#0000ff](swatches/0000ff.svg) |
| Yellow | `#ffff00` | ![#ffff00](swatches/ffff00.svg) |
| Cyan | `#00ffff` | ![#00ffff](swatches/00ffff.svg) |
| Magenta | `#ff00ff` | ![#ff00ff](swatches/ff00ff.svg) |
| White | `#ffffff` | ![#ffffff](swatches/ffffff.svg) |
| Grey | `#808080` | ![#808080](swatches/808080.svg) |
| Black | `#000000` | ![#000000](swatches/000000.svg) |

## Usage

Add to your workflow:

```yaml
- uses: andornaut/color-swatches-action@main
  with:
    files: 'README.md'
    out-dir: 'swatches'
```

### Inputs

| Input | Description | Default |
| ----- | ----------- | ------- |
| `files` | Space-separated files to extract colors from (supports globs) | *required* |
| `format` | Output format: `svg` or `png` | `svg` |
| `out-dir` | Output directory for swatch files | *required* |
| `commit` | Whether to commit and push generated swatches | `true` |

### Example workflow

```yaml
name: Generate color swatches

on:
  push:
    branches: [main]
    paths:
      - README.md

  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: andornaut/color-swatches-action@main
        with:
          files: 'README.md'
          out-dir: 'swatches'
```

## CLI / npm usage

The script can be installed as an npm package directly from GitHub:

```bash
npm install github:andornaut/color-swatches-action
```

Then run it via npx or an npm script:

```bash
npx color-swatches [--format svg|png] <out-dir> <file...>
```

Example npm script in `package.json`:

```json
{
  "scripts": {
    "swatches": "color-swatches --format png swatches themes/*.json"
  }
}
```

## Quick setup

Run the setup script to generate the workflow file in your repo:

```bash
bash <(curl -s https://raw.githubusercontent.com/andornaut/color-swatches-action/main/setup.sh) \
  --files 'README.md' \
  --out-dir 'swatches'
```

## Referencing swatches in markdown

The generated files are named by hex value (e.g. `ff0000.svg` or `ff0000.png`). Reference them in your README:

```markdown
| Color | Hex | Usage |
| ----- | --- | ----- |
| ![red](swatches/ff0000.svg) Red | `#ff0000` | Primary |
```

## License

[MIT](LICENSE)
