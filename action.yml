name: Color Swatches
description: Extract hex colors from files and generate color swatches

inputs:
  out-dir:
    description: Output directory for swatch files
    required: true
  files:
    description: Space-separated list of files to extract colors from (supports globs)
    required: true
  format:
    description: "Output format: svg or png"
    required: false
    default: "svg"
  commit:
    description: >-
      Commit the generated swatches and push them to the branch. Off by
      default: writing to a repository is what a workflow asks for rather than
      what it gets for calling this, and the job needs contents: write for it.
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: Generate swatches
      shell: bash
      run: |
        file_args=(${{ inputs.files }})
        node "${{ github.action_path }}/color-swatches.mjs" --format "${{ inputs.format }}" "${{ inputs.out-dir }}" "${file_args[@]}"

    - name: Commit swatches
      if: inputs.commit == 'true'
      shell: bash
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add "${{ inputs.out-dir }}/"
        git diff --cached --quiet && echo "No changes" && exit 0
        git commit -m "[skip ci] Generate color swatches"
        git push
